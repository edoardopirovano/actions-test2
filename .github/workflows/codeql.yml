name: "Code Scanning - CodeQL"

on:
  pull_request:
  push:

jobs:
  codeql:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@main
      - name: Initialize CodeQL AGAIN
        uses: github/codeql-action/init@main
